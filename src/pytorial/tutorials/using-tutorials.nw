\chapter{Tutorial: Using Tutorials}
\label{tutorial-using-tutorials}

[[using-tutorials]] is the entry point for a learner picking up the
package for the first time.  It exercises the three CLI verbs a learner
meets in every setting ([[list]], [[run]], [[review]]) and every step
backend (shell, editor, [[input]], [[single_select]], [[multi_select]])
in one linear walk-through, so a learner who finishes it has touched the
whole learner-facing surface at least once.

It is also the \emph{universal} tutorial: a host application that embeds
the CLI gets this lesson mounted ahead of its own tutorials, and nothing
else from the package (see [[embedded_tutorial_paths]]).  That gives the
lesson one constraint the other built-ins do not have.  It may depend
only on what every catalog is guaranteed to contain, which is
[[using-tutorials]] itself, and only on verbs every host exposes.  So it
names no other tutorial, and it leaves out [[install]], which hosts hide
because they do not read the user's installed tutorial directory;
[[writing-tutorials]] teaches that verb instead, to the authors who need
it.  An earlier version broke both rules, and no learner in a host could
get past its first step.

The internal arc is roughly:
\begin{enumerate}
\item discover what is available (steps 1--3 cover [[list]] and [[run]]
  with shell steps),
\item author content into the workspace (step 4 introduces the editor
  backend),
\item answer questions of increasing structure (steps 5--7 cover the
  three [[kind]] values), and
\item inspect what was recorded (steps 8--9 cover [[review]] with and
  without [[--step]]).
\end{enumerate}
This shape is variation theory's separation pattern at the tutorial
level: the workflow stays invariant across the steps, while one aspect
of the CLI rotates through being practised.

\section{Tutorial source}

The whole tutorial file is one tangled root chunk.  Each step lives in
its own named sub-chunk so the prose between them can describe that
step's role without breaking the markdown output.

<<[[using-tutorials.md]]>>=
---
id: using-tutorials
title: Using Tutorials
summary: Learn how list, run, question prompts, and review fit together.
---

<<list step>>

<<run help step>>

<<retry shell step>>

<<capture workflow step>>

<<input question step>>

<<single select step>>

<<multi select step>>

<<review run step>>

<<review one step step>>
@

\section{Step 1: List the available tutorials}

We open with [[tutorial list]] because it is the only command a learner
can run before anything else exists --- it has no required arguments
and produces output even on a fresh install.  Besides the typed command,
the required patterns name exactly one tutorial: [[using-tutorials]].
That id is the only one present in every catalog, standalone or
embedded, and it is present by construction, since the learner is
running it.  Requiring it still makes the validator check the listing's
\emph{content} rather than the mere presence of output.  Requiring the
other built-ins as well, as this step once did, checks more in the
standalone CLI but can never pass in a host, where they are not mounted.

The prose mentions that the same command emits a TSV when its stdout is not
a terminal: this is the only place in the tutorial corpus where that
detail surfaces, and it matters for users who script around the CLI.

<<list step>>=
# List the available tutorials

```tutorial-step
required_patterns:
  - mode: regex
    pattern: (?:^|\s)(?:tutorial|pytorial)\s+list\b
  - using-tutorials
hint: "One verb prints the catalog of available tutorials --- its name says what it does."
```

Use the **list** verb.

In a terminal, the **list** verb renders a readable table. When the
same output is piped into a script, it switches to tab-separated rows.

Its main job is to show which tutorials are available and how much
progress is already saved for each one.
@

\section{Step 2: Read the help for run}

Step~1 showed what can be run; this step introduces the verb that runs
it, together with its one flag a learner needs early: [[--restart]].
The learner meets both by asking [[run]] for its own help.

The obvious alternative is to have the learner start a second tutorial
from inside this one, and an earlier version did exactly that with
[[shell-basics]].  It cannot survive the host-agnostic constraint: a
host mounts only this lesson plus its own tutorials, so there is no
second tutorial we can name.  Pointing the learner at [[using-tutorials]]
itself does not work either.  Without [[--restart]] the nested [[run]]
would resume the very step that launched it.  With [[--restart]] it
starts a \emph{new} run of this tutorial, which becomes the latest one;
the [[review]] in step~8 reads the latest run, so it would find a nearly
empty record instead of the steps the learner has completed, and a later
resume would pick the wrong run too.  Reading the help has none of these
hazards, because it starts nothing.

\begin{description}
\item[The typed-command pattern] allows a leading host name, so
  [[canvaslms tutorial run --help]] validates as well as the bare
  standalone spelling.
\item[The literal [[--restart]] ] can only come from the help output,
  since the instructions never ask the learner to type it.  Both
  frontends print it verbatim: Typer in its option table and
  [[argparse]] in its usage line, and the transcript matcher already
  strips the styling around it.
\end{description}

The instructions say precisely what [[--restart]] does to saved work:
it begins a new run and leaves earlier runs in place, reachable through
[[review --run-id]].  This lesson used to claim that the flag discards
every saved transcript, which is false and would have made a careful
learner afraid of a harmless flag.

<<run help step>>=
# Read the help for run

```tutorial-step
required_patterns:
  - mode: regex
    pattern: (?:^|\s)(?:tutorial|pytorial)\s+run\s+--help\b
  - "--restart"
hint: "Every verb describes itself when you add `--help`. Ask the verb that starts tutorials."
```

Use the **run** verb with `--help`, and find the `--restart` flag in
what it prints.

The **run** verb starts a tutorial, or resumes it at the first
unfinished step if you have saved progress. That is how you got here.

`--restart` begins a fresh run from step 1 instead of resuming. Your
earlier runs are kept, not deleted: the **review** verb can still show
them, as you will see later in this lesson.

Every verb answers `--help` the same way, so this is also how you look
up anything this lesson does not cover.
@

\section{Step 3: Retry a shell step}

The first two steps can hardly fail, so this one makes failing part of
the exercise in order to introduce \enquote{try again}.  The pedagogical
move is two-pass:
the learner runs only [[pwd]], fails, then runs both [[pwd]] and [[ls]]
on the second attempt.  Validating only the second attempt
([[required_patterns]] lists both commands) demonstrates the
\enquote{only the latest recorded attempt counts} rule without
spelling it out as a separate concept --- the learner experiences it
directly.

Anchoring on the [[using-tutorials $]] prompt checks that the commands
were typed in this tutorial's own shell.  It is also where the lesson
now teaches prompt relabelling.  That rule used to emerge from a
contrast with a nested [[shell-basics $]] prompt in step~2; with the
nested run gone the contrast is gone too, so the instructions state the
rule outright.  A learner who later runs a host's tutorial sees the
other prompt there and can complete the generalisation themselves.

<<retry shell step>>=
# Retry a shell step

```tutorial-step
required_patterns:
  - "using-tutorials $"
  - pwd
  - ls
hint: "Two passes: feel the not-complete message after a partial attempt, then take the runner's retry offer and finish every command the step requires."
```

Look at the prompt of this shell: `using-tutorials $ `. Every tutorial
labels its shell with its own id, so you can always tell which tutorial
owns the shell you are typing in.

To feel how step validation works, try this step in two rounds:

1. First run only `pwd`, then exit the shell.
2. The tutorial should say the step is not complete yet.
3. Choose `try again`, run both `pwd` and `ls`, then exit again.

Only the latest recorded attempt counts. If you leave now and come back
later, the **run** verb resumes from the first unfinished step unless
you restart it.
@

\section{Step 4: Capture the workflow in a file}

Switching backends here without changing the underlying validation
idea is variation theory's contrast pattern: same authoring shape
([[required_patterns]] over the recorded artefact), different artefact
(an editor-saved file rather than a shell transcript).  The three
required patterns are exactly the three lines the learner is asked to
type, which doubles as a checklist they can self-assess against
before saving.  Putting the editor step here also breaks up the
sequence of shell steps, which would otherwise risk a learner
mistakenly inferring that the runner is shell-only.

<<capture workflow step>>=
# Capture the workflow in a file

```tutorial-step
edit_file: tutorial-commands.txt
required_patterns:
  - list discovers tutorials
  - run starts or resumes one
  - review shows saved transcripts
hint: "Each line names one verb and what it is for --- focus on the role, not on how to spell the command."
```

Open `tutorial-commands.txt` in your editor and add these three lines:

- `list discovers tutorials`
- `run starts or resumes one`
- `review shows saved transcripts`

Save the file and quit the editor.

This contrasts a shell step with an editor step while keeping the same
validation idea: the tutorial only checks what was recorded after you
finish the step.
@

\section{Step 5: Answer a one-line question}

This is the smallest question step possible: [[kind: input]] with a
single regex answer.  The regex accepts either bare [[tutorial]] /
[[pytorial]] commands or a one-token host prefix followed by one of
those names.  It still tolerates trailing whitespace so terminal
autocomplete does not fail the match.  We chose regex rather than a
[[contains]] string for this introduction so the learner sees the more
powerful answer mode first; the next two steps then \emph{contrast}
with bare-string answers, which makes the difference between answer
modes legible through variation rather than through direct explanation.

<<input question step>>=
# Answer a one-line question

```tutorial-step
kind: input
answers:
  - mode: regex
    pattern: ^(\S+\s+)?(?:tutorial|pytorial)\s+review\s+using-tutorials\s*$
hint: "Which verb reads saved transcripts? Pair it with the id of the tutorial you are inside right now."
```

Type the exact command that reviews this tutorial.

This is the smallest question step. The tutorial asks for one line of
text, records it immediately, and validates it without opening a shell.
@

\section{Step 6: Pick the command that starts work}

[[single_select]] is the simplest of the multiple-choice kinds.
Crucially, the [[answers]] list contains the literal option text, not
an index, so authoring stays robust under option re-ordering.  The
prose highlights that the runner accepts both the option number
\emph{and} the option text from the learner --- this is a
learner-facing convenience, but the authored answer always uses the
text form so authors do not have to track positions.

<<single select step>>=
# Pick the command that starts work

```tutorial-step
kind: single_select
options:
  - list
  - run
  - review
answers:
  - run
hint: "Look for the verb whose job is to begin or resume work, not to inspect it."
```

Choose the verb that starts or resumes one tutorial.

Single-select questions show numbered options. You can answer with the
number or with the full option text.
@

\section{Step 7: Pick every read-only tutorial command}

[[multi_select]] generalises [[single_select]] to multiple correct
options.  The distractors are [[run]] and [[run --restart]].  The second
one earns its place by reusing the flag from step~2: a learner who read
there that a restart \emph{keeps} earlier runs may conclude it changes
nothing, and the question corrects that, since it still starts a run.
It also has to be something every host offers; the distractor used to be
[[install]], which a learner in a host has never seen.  The framing
question (\enquote{commands that only read}) is the conceptual axis the
step is testing, and the [[hint]] re-states the axis explicitly because
the option labels alone do not signal read-versus-write semantics.

<<multi select step>>=
# Pick every read-only tutorial command

```tutorial-step
kind: multi_select
options:
  - list
  - run
  - run --restart
  - review
answers:
  - list
  - review
hint: "Pick the verbs that only read --- a run started afresh is still a run."
```

Choose every option here that reads existing information without
starting a run.

Multi-select questions also use literal option text. The learner may pick
the correct options in any order, but the tutorial still stores the
authored answer list.
@

\section{Step 8: Review the saved run}

This step is the first time [[tutorial review]] appears, and it does
double duty: the learner runs the command, and the [[required_patterns]]
list verifies that the review output covers earlier steps the learner
has already finished.  We list specific step headings (\enquote{Step 1:
\ldots}, \enquote{Step 4: \ldots}, \enquote{Step 7: \ldots}) rather
than every step, because the review command shows only steps recorded
\emph{before} the current one.  Picking widely separated indices
exercises the review's coverage without being brittle to the ordering
of recently completed steps.  The trailing prose summarises the
workflow seen so far, which is the variation-theory \emph{fusion} move:
after exposing the parts (list, run, question kinds), we step back and
fuse them into a single mental model.

<<review run step>>=
# Review the saved run

```tutorial-step
required_patterns:
  - mode: regex
    pattern: (?:^|\s)(?:tutorial|pytorial)\s+review\s+using-tutorials\b
  - Using Tutorials
  - "Step 1: List the available tutorials"
  - "Step 4: Capture the workflow in a file"
  - "Step 7: Pick every read-only tutorial command"
  - review shows saved transcripts
hint: "Use the read-only verb on this very tutorial --- and stay inside the current shell so the run is still in progress when you read it back."
```

Use the **review** verb on `using-tutorials`.

Review reads the saved transcripts for the current run. Because this shell
session is still in progress, the review output can show the earlier saved
steps, but it cannot show this step until after you exit the shell and the
step is recorded.

At this point you have seen the main run-and-review workflow end to end:

- `list` discovers what you can run
- `run` starts or resumes one tutorial at a time
- question steps can ask for input, one choice, or several choices
- `review` inspects the saved record afterwards
@

\section{Step 9: Inspect one step of the saved run}

The narrowing flag [[--step N]] is taught immediately after the
unfiltered review so the contrast is direct.  The required patterns
verify that exactly one step's heading appears in the output, which is
how a learner sees that the flag actually narrows the view rather than
just highlighting a step within the full review.  We also mention
[[--run-id]] in the prose --- not as a pattern --- because that flag
selects an older run, and exercising it would require the learner to
have an older run to point at, which is out of scope for a linear
tutorial.

<<review one step step>>=
# Inspect one step of the saved run

```tutorial-step
required_patterns:
  - mode: regex
    pattern: (?:^|\s)(?:tutorial|pytorial)\s+review\s+using-tutorials\s+--step\s+1\b
  - "Step 1: List the available tutorials"
hint: "One flag narrows the review to a single step; combine it with the right index."
```

Use the **review** verb with `--step 1`.

`--step N` narrows the review to one 1-indexed step instead of the whole
run. Review normally shows the latest saved run for that tutorial; use
`--run-id <id>` to pick an older one instead. The help for **review**
shows the full form.
@
